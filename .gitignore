# Virtual environments
.venv/
.fullenv/
.toxenv/

# Build artifacts
build/
dist/
*.so
*.egg-info/
treesitter_chunker.egg-info/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Test & coverage
.coverage
.pytest_cache/
pytest_cache/
pytest_*.log
*.log
mcp_server.log
*.db
ide/**/node_modules/

# Generated files
generated_config.yaml
generated_config.toml
generated_config.json
worktrees/

# Grammar repos (cloned fresh by scripts/fetch_grammars.py, not tracked)
grammars/
tree-sitter-headers/

# Test repositories (downloaded for testing)
TypeScript/
click/
flask/
gin/
googletest/
guava/
lodash/
ruby/
rust/
serde/

# Local caches
__pycache__/

# Environment files (secrets)
.env
.env.*
*.env
.*.env
!.env.example
.envrc
.flaskenv

# Common private keys/certs
*.pem
*.key
*.crt

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Phase execution artifacts
.worktrees/
.claude/run-logs/
.claude/ai-dev-kit/
logs/
.pubenv/
.stow-symbols.json
/.dev-skills/

# Boundary IR golden fixtures are first-class committed test data. The
# "test repositories (downloaded for testing)" patterns above (e.g. ruby/,
# rust/) would otherwise swallow same-named fixture dirs. This negation is last
# so it wins (git applies the last matching pattern).
!tests/fixtures/boundary_ir/repos/
!tests/fixtures/boundary_ir/repos/**
