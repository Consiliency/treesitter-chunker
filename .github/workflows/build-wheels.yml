name: Build wheels

on:
  push:
    tags:
      - "v*"
  workflow_dispatch: {}

jobs:
  build-wheels:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.12"]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install cibuildwheel
        run: |
          python -m pip install --upgrade pip
          python -m pip install cibuildwheel

      - name: Build wheels with grammars
        env:
          CHUNKER_WHEEL_LANGS: python,javascript,rust
          CHUNKER_GRAMMAR_BUILD_DIR: ${{ github.workspace }}/chunker/data/grammars/build
          CIBW_ENVIRONMENT: CHUNKER_GRAMMAR_BUILD_DIR=${{ github.workspace }}/chunker/data/grammars/build
          CIBW_BEFORE_BUILD: pip install tree_sitter && python scripts/fetch_grammars.py && python scripts/build_lib.py
        run: |
          mkdir -p chunker/data/grammars/build
          python -m cibuildwheel --output-dir wheelhouse

      - name: Upload wheel artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.os }}
          path: wheelhouse/*
