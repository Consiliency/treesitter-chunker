Historical specifications live here.

These specs informed earlier implementation phases, but they may not match the current codebase or release workflows.
Use the active files under `specs/` for current guidance.
