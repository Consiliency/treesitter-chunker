Archived scripts live here.

These files were useful during earlier cleanup, grammar-build, and QA campaigns, but they are not part of the current supported toolchain.
Prefer the active scripts that remain under `scripts/` and the release workflows under `.github/workflows/`.
